<?xml version="1.0" encoding="utf-8"?>
<!--
  BRAND.md §3 — the themed-icon layer (Android 13+). The mark's silhouette only: the system
  tints it from the user's wallpaper palette, so the colour declared here is discarded and
  only the shape matters. It cannot reuse the foreground layer, whose stroke colour is fixed
  brown and would fight every wallpaper it landed on.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="108dp"
    android:height="108dp"
    android:viewportWidth="108"
    android:viewportHeight="108">
    <path
        android:pathData="M66.22,36.55 A13,13 0 1,0 54,54 A13,13 0 1,1 41.78,71.45"
        android:strokeColor="#000000"
        android:strokeWidth="12"
        android:strokeLineCap="round" />
</vector>
